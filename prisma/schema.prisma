generator client {
  provider = "prisma-client"
  output   = "../src/generated"
}

datasource db {
  provider = "postgresql"
}

model task {
  id   Int    @id @default(autoincrement())
  desc String
}
